{{- /*
  金丝雀 Ingress：使用 nginx.ingress.kubernetes.io/canary 注解按权重分流
  weight=10 → 10% 流量进入金丝雀版本，验证稳定后再逐步调高
*/ -}}
{{- if .Values.canary.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "cloudforge.fullname" . }}-canary
  labels:
    {{- include "cloudforge.labels" . | nindent 4 }}
  annotations:
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-weight: {{ .Values.canary.weight | quote }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "cloudforge.fullname" . }}-canary
                port:
                  number: {{ .Values.service.port }}
{{- end }}
